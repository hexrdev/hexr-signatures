# Pattern: ReAct (Yao et al. 2022)
# Loop where LLM calls are interleaved with tool calls (Reason → Act → Observe).
schema_version: "1.0"
kind: pattern
id: react
display_name: "ReAct"
homepage: https://arxiv.org/abs/2210.03629
maintainer: hexr
version: "1.0.0"

match:
  has_loop: true
  min_llm_calls_per_function: 1
  min_tool_calls_per_function: 1

confidence: 0.8
